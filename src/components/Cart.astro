---
// Este componente es intencionalmente visual y no tiene lógica de estado.
// Representa un "carrito en construcción" como lo pide la rúbrica.
---

<!-- Overlay y Sidebar del Carrito -->
<div id="cart-sidebar" class="fixed inset-0 bg-gray-900 bg-opacity-50 backdrop-blur-sm z-50 hidden" aria-labelledby="cart-heading" role="dialog" aria-modal="true">
  <div class="absolute inset-y-0 right-0 w-full max-w-md bg-white shadow-xl">
    <div class="flex flex-col h-full">
      <!-- Header del Carrito -->
      <div class="flex items-center justify-between px-6 py-4 border-b border-gray-200">
        <h2 id="cart-heading" class="text-lg font-semibold text-gray-800">Mi Carrito</h2>
        <button id="cart-close-btn" type="button" class="p-2 text-gray-500 hover:text-gray-800 hover:bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" aria-label="Cerrar carrito">
          <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>

      <!-- Contenido del Carrito (Estado Vacío) -->
      <div class="flex-1 flex flex-col items-center justify-center text-center px-6">
        <svg class="w-24 h-24 text-gray-300 mb-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
        </svg>
        <h3 class="text-xl font-semibold text-gray-700">Tu carrito está vacío</h3>
        <p class="mt-2 text-gray-500">Los productos que añadas aparecerán aquí.</p>
      </div>

      <!-- Footer del Carrito -->
      <div class="px-6 py-4 border-t border-gray-200">
        <button type="button" disabled class="w-full bg-gray-300 text-gray-500 font-bold py-3 px-4 rounded-lg cursor-not-allowed">
          Proceder al Pago
        </button>
      </div>
    </div>
  </div>
</div>
